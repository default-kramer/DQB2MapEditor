<Window x:Class="MinimapEditor.ExportImageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MinimapEditor"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        Title="Export Image">
    <StackPanel Orientation="Vertical" Margin="12">
        <Image Source="{Binding PreviewImage4497}" Width="500" Height="500"
               Stretch="Uniform" HorizontalAlignment="Center"></Image>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <RadioButton IsChecked="{Binding IsNoCrop7138}">Full Image</RadioButton>
            <RadioButton IsChecked="{Binding IsAutoCrop3562}" Margin="18,0,0,0">Auto Crop</RadioButton>
            <RadioButton IsChecked="{Binding IsCustomCrop1422}" Margin="18,0,0,0">Current Crop</RadioButton>
        </StackPanel>
        <Grid Margin="0,8,0,0" Width="500">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SaveToPath3518, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Button Grid.Column="1" Margin="4,0,0,0" Command="{Binding CommandBrowse6020}">Browse</Button>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Grid VerticalAlignment="Center">
                <TextBlock Text="{Binding PlaceholderDimensions1371}" Visibility="Hidden"></TextBlock>
                <TextBlock Text="{Binding Dimensions6326}"></TextBlock>
            </Grid>
            <Button Margin="18,0,0,0" Padding="3" FontSize="14" Command="{Binding CommandExport1298}">Export Image</Button>
            <Button Margin="18,0,0,0" Padding="3" FontSize="14" IsCancel="True">Close</Button>
        </StackPanel>
    </StackPanel>
</Window>
